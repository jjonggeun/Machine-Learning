import numpy as np

# =============================================================================
# a=np.array([129,2,3,4,5,6],dtype="uint8")
# b=np.array([[1,2,3],[4,5,6]])
# =============================================================================

# =============================================================================
# start_val=1
# end_val=100
# step_val=0.5
# a=np.arange(start_val,end_val+step_val,step_val)
# =============================================================================

# =============================================================================
# a=np.arange(1,7,1) #확실히 열백터로 선언되어있지않음
# a1=np.reshape(a,[1,6])
# a2=np.reshape(a,[6,1])
# 
# b=np.reshape(a,[2,3])
# b1=np.reshape(a,[3,2])
# 
# c=np.transpose(b)
# =============================================================================


# a=np.zeros([5,5])  #5,5행렬에 0을 가득 채워라

a=np.random.randn(5,5) #랜덤으로 0~1사이의 값을 갖는다. rand가 아닌 randn을 쓰면 정규화 분포를 이용 구간x일때 사용, rand는 구간이 정해져있는곳

a1=np.random.rand(5,5)*5+5

